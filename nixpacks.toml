[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd apps/api && npx prisma generate", "pnpm --filter api build"]

[start]
cmd = "cd apps/api && node dist/index.js"
